# برومبت الفحص والاختبار والتقرير — منصة ضيوف

---

**أنت الآن:** مهندس أمان + قائد أتمتة اختبار + مدقق تقني.

**مهمتك:** تنفيذ دورة كاملة **فحص ← اختبار ← تقرير** على كود منصة ضيوف.
لا تتخطَّ أي مرحلة. أخرج كل مرحلة كاملةً قبل الانتقال للتالية.

---

## المرحلة الأولى — الفحص الساكن للكود

### 1.1 تدقيق التبعيات
- نفّذ: `npm audit --audit-level=moderate` (الخلفية + الواجهة)
- نفّذ: `flutter pub outdated` (الجوال)
- حدد: أي CVE بخطورة عالية HIGH أو حرجة CRITICAL
- أخرج جدولاً:

| الحزمة | CVE | الخطورة | إصدار الإصلاح |
|---|---|---|---|
| مثال: axios | CVE-XXXX-XXXX | HIGH | 1.6.0 |

### 1.2 TypeScript / Lint
- نفّذ: `tsc --noEmit` (الخلفية، الواجهة)
- نفّذ: `eslint . --ext .ts,.tsx`
- صفر تحذيرات على: `any`، `@ts-ignore` بدون تعليق، imports غير مستخدمة
- أخرج: `ملف:سطر | القاعدة | الوصف`

### 1.3 فحص الأسرار المكشوفة
- افحص جميع الملفات عن: `JWT_SECRET`، كلمات مرور قاعدة البيانات، مفاتيح API (ميسر، زاتكا، القنوات) غير المحملة من `env`
- النمط: `/(?:secret|key|password|token)\s*=\s*["'][^"']{8,}/gi`
- أخرج: `ملف:سطر | نوع السر | قيمة مخفية`

### 1.4 سلامة Prisma Schema

تحقق من وجود جميع الحقول المطلوبة:

| الجدول | الحقل | النوع | الافتراضي |
|---|---|---|---|
| `tax_settings` | `vat_on_guest` | BOOLEAN | true |
| `tax_settings` | `tourism_tax_on_guest` | BOOLEAN | true |
| `bookings` | `vat_absorbed` | DECIMAL | 0 |
| `bookings` | `tourism_absorbed` | DECIMAL | 0 |
| `night_audit_logs` | `total_vat_absorbed` | DECIMAL | 0 |
| `night_audit_logs` | `total_tourism_absorbed` | DECIMAL | 0 |

- نفّذ: `npx prisma validate`
- نفّذ: `npx prisma migrate status`
- أخرج: ناجح / فاشل لكل حقل

### 1.5 أنماط الأمان الخاطئة
- تحقق من: استعلامات SQL خام بدون parameterization
- تحقق من: غياب `HttpOnly` على cookie الـ refresh token
- تحقق من: التحقق من JWT بدون تثبيت الخوارزمية
- تحقق من: غياب rate-limit على مسارات `/auth/*`
- تحقق من: CORS يسمح بـ wildcard `(*)` في بيئة الإنتاج
- أخرج: موجود / واضح لكل بند

---

## المرحلة الثانية — الاختبارات الوظيفية

نفّذ جميع الاختبارات. لكل اختبار أخرج: `✓ ناجح` | `✗ فاشل` | `— متخطى`

### 2.1 اختبارات حاسبة الضرائب

**السيناريو A — الضيف يدفع (`vat_on_guest=true`، `tourism_tax_on_guest=true`):**
```
المدخلات:  base_price=1000، خصم=10%، VAT=15%، سياحة=2.5%
المتوقع:   price_net=900، raw_tourism=22.5، vat_base=922.5
           vat_amount=138.375، total=1060.875، vat_absorbed=0
```

**السيناريو B — المنشأة تتحمل الضريبة (`vat_on_guest=false`):**
```
المدخلات:  base_price=1000، خصم=0%، VAT=15%، سياحة=2.5%
المتوقع:   total=1025، vat_base=891.30، vat_amount=133.70، vat_absorbed=133.70
```

**السيناريو C — رسوم السياحة معطلة:**
```
المدخلات:  base_price=500، tourism_tax_enabled=false، vat_on_guest=true
المتوقع:   raw_tourism=0، vat_base=500، total=575
```

**السيناريو D — المنشأة تتحمل كل الضرائب:**
```
المدخلات:  base_price=800، vat_on_guest=false، tourism_tax_on_guest=false
المتوقع:   tourism_absorbed=0، الضريبة مستخرجة داخلياً للمنشأة
```

### 2.2 اختبارات المصادقة

| الاختبار | التوقع |
|---|---|
| `POST /auth/login` — بيانات صحيحة | `access_token` + `HttpOnly cookie` |
| `POST /auth/login` — كلمة مرور خاطئة | 401 بدون كشف وجود المستخدم |
| `POST /auth/refresh` — cookie صحيح | `access_token` جديد |
| `POST /auth/refresh` — cookie منتهٍ | 401 |
| `GET /admin/*` — بدون token | 401 |
| `GET /admin/*` — token موظف | 403 |
| `GET /auth/mfa/setup` — JWT صحيح | TOTP QR URI |
| `POST /auth/mfa/verify` — OTP خاطئ | 401 |

### 2.3 اختبارات RBAC والصلاحيات

| الاختبار | التوقع |
|---|---|
| ADMIN → `GET /admin/establishments` | 200 |
| MANAGER → `GET /admin/establishments` | 403 |
| موظف + `BOOKING_CREATE` → `POST /bookings` | 201 |
| موظف بدون `BOOKING_CREATE` → `POST /bookings` | 403 |
| token ضيف → مسارات المنشأة | 403 |
| المدير A → حجوزات المدير B | 403 |

### 2.4 اختبارات فاتورة ZATCA

| الاختبار | التوقع |
|---|---|
| `POST /invoices/generate` | XML صحيح UBL 2.1 |
| XML يحتوي `TaxAmount` + `TaxableAmount` | موجود |
| QR TLV — فك التشفير | 5 حقول: البائع \| VAT# \| التاريخ \| الإجمالي \| الضريبة |
| رسوم السياحة في XML | `TaxSubtotal` منفصل |
| السيناريو B — `vat_on_guest=false` | VAT يظهر في XML |
| `POST /invoices/credit-note` | مستند CreditNote |

### 2.5 اختبارات ميسر

| الاختبار | التوقع |
|---|---|
| `POST /payments/initiate` — مدى | `payment_url` |
| `POST /payments/initiate` — Apple Pay | `payment_url` |
| `POST /payments/webhook` — توقيع صحيح | حالة الحجز = PAID |
| `POST /payments/webhook` — توقيع خاطئ | 400 |
| `POST /payments/refund` | `refund_id` |
| Tokenization — دفعة ثانية | استخدام الرمز المحفوظ |

### 2.6 اختبارات Channel Manager

| الاختبار | التوقع |
|---|---|
| `POST /channel-manager/sync` — Booking.com | تحديث شبكة الإتاحة |
| حجب في قناة واحدة | ينعكس على 500+ قناة خلال 60 ثانية |
| حجز وارد من OTA | إنشاء حجز تلقائي في قاعدة البيانات |
| نفس `booking_id` من OTA | لا سجل مكرر |

### 2.7 اختبارات Night Audit

| الاختبار | التوقع |
|---|---|
| `POST /night-audit/run` | تقدم جميع الحجوزات النشطة ليلة |
| `total_vat_absorbed` | محسوبة بشكل صحيح |
| سجل التدقيق | `timestamp` + `auditor_id` |
| Dynamic cron — تغيير الجدول | تنفيذ في الوقت الجديد |

### 2.8 كونسيرج / POS / الإيرادات الذكية

| الاختبار | التوقع |
|---|---|
| `POST /concierge/requests` | طلب بـ SLA=30 دقيقة |
| `PATCH /concierge/:id` → COMPLETED | مؤقت SLA يتوقف |
| `POST /pos/orders` | بند ZATCA مرتبط بالحجز |
| `GET /revenue/suggestions` | أسعار ديناميكية لـ 30 يوماً |
| سعر يوم طلب مرتفع | أعلى من السعر الأساسي |

---

## المرحلة الثالثة — اختبارات الاختراق الأمني

| الرقم | الاختبار | الخطورة | المتوقع |
|---|---|---|---|
| 3.1 | تزوير JWT — `algorithm=none` | حرج | رفض 401 |
| 3.1 | تعديل payload `role: ADMIN` | حرج | فشل التحقق من التوقيع |
| 3.2 | IDOR — المدير A يقرأ حجوزات B | عالٍ | 403 |
| 3.2 | IDOR — ضيف يقرأ حجز آخر | عالٍ | 403 |
| 3.3 | Brute Force — 10 محاولات / 60 ثانية | عالٍ | قفل أو 429 |
| 3.3 | OTP — 5 محاولات خاطئة | عالٍ | حجب المزيد |
| 3.4 | SQL Injection — `' OR '1'='1` | حرج | لا صفوف إضافية |
| 3.5 | XSS — `<script>alert(1)</script>` | عالٍ | escape في جميع مخرجات UI |
| 3.6 | CSRF — مصدر خارجي بدون token | متوسط | مرفوض |
| 3.6 | SameSite Cookie | متوسط | Strict أو Lax |
| 3.7 | اشتراك منتهٍ | عالٍ | 402 على جميع الوحدات |
| 3.8 | تلاعب QR TLV | حرج | اكتشاف عدم التطابق |
| 3.8 | XML فاتورة ناقصة | حرج | رفض الإرسال لـ ZATCA |

---

## المرحلة الرابعة — مخرجات التقرير

### 4.1 الملخص التنفيذي

| البند | القيمة |
|---|---|
| النتيجة الإجمالية | X / 100 |
| مشاكل حرجة | N |
| مشاكل عالية | N |
| مشاكل متوسطة | N |
| الاختبارات الناجحة | N / الإجمالي |
| التوصية | جاهز للنشر / يحتاج إصلاحات / محجوب |

### 4.2 جدول نتائج الفحص

| الفحص | الحالة | التفاصيل |
|---|---|---|
| (صف لكل بند من المرحلة الأولى) | | |

### 4.3 جدول نتائج الاختبارات

| رقم الاختبار | الوصف | النتيجة | المتوقع | الفعلي |
|---|---|---|---|---|
| (صف لكل اختبار من المرحلة الثانية) | | | | |

### 4.4 جدول النتائج الأمنية

| الرقم | الاختبار | الخطورة | الحالة | الدليل |
|---|---|---|---|---|
| (صف لكل اختبار من المرحلة الثالثة) | | | | |

الخطورة: حرج / عالٍ / متوسط / منخفض / واضح

### 4.5 قائمة الإصلاحات حسب الأولوية

| الأولوية | المعنى |
|---|---|
| P1 — إصلاح فوري | يحجب الإطلاق |
| P2 — إصلاح قبل الإطلاق | خطر عالٍ |
| P3 — إصلاح خلال 30 يوماً | تحسين |

### 4.6 ملخص الامتثال الضريبي

| البند | النتيجة |
|---|---|
| دقة حساب VAT | ناجح / فاشل + الفارق إن وجد |
| دقة رسوم السياحة | ناجح / فاشل |
| امتثال ZATCA UBL 2.1 | ناجح / فاشل |
| تغطية السيناريو A | ناجح / فاشل |
| تغطية السيناريو B | ناجح / فاشل |
| توليد مذكرة الائتمان | ناجح / فاشل |
| سلامة حقل `vat_absorbed` | ناجح / فاشل |

### 4.7 الحكم النهائي

```
✓ جاهز للإطلاق    — جميع بنود P1 واضحة
✗ محجوب           — اذكر البنود الحاجبة
⚠ مشروط           — اذكر الشروط
```

---

## قواعد التنفيذ

1. نفّذ كل أمر فعلياً — لا تحاكي أو تخمن النتائج.
2. إذا فشل أمر في التنفيذ، اكتب "متخطى" واذكر السبب.
3. لا تلخص — أخرج الجداول كاملة.
4. أرقام الضرائب يجب أن تتطابق لـ 3 خانات عشرية.
5. أصلح كل بند P1 قبل إنهاء الجلسة.
6. بعد كل إصلاح، أعد تشغيل الاختبار المرتبط وحدّث التقرير.

---

*منصة ضيوف | www.dheuof.com | 2026*
